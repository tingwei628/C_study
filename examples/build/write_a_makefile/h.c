// #include <stdio.h>
#include "hello.h"
int main() {
 /* int a, b, temp;
  printf("input a number\n");
  scanf("%d", &a);
  scanf("%d", &b);
  while((a % b)!= 0) {
    temp = a % b;
    a = b;
    b = temp;
  }
  printf("%d\n", b);
  */
  hello("world");
  return 0;
}
